{% extends 'base.html.twig' %}
{% block body %}


        
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container-fluid">
                <a class="navbar-brand">Biblioteca particular</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item"><a class="nav-link active" href="{{path('inicio')}}">inicio</a></li>
                        <li class="nav-item"><a class="nav-link active" href="{{path('listar_libros')}}">ver libros</a></li>
                        <li class="nav-item"><a class="nav-link active" href="{{path('nuevo')}}">Nuevo</a></li>
                        <li class="nav-item"><a class="nav-link active" href="{{path('insertar_editorial')}}">InsertarConEditorial</a></li>
                        <li class="nav-item"><a class="nav-link active" href="{{path('filtrar', {'paginas': 100})}}">Libros(<=100p.)</a></li>
                        <li class="nav-item"><a class="nav-link active" href="{{path('filtrar', {'paginas': 200})}}">Libros(<=200p.)</a></li>
                    </ul>
                </div>
            </div>
        </nav>

    <div class="container main-container mt-5">
        {% block content %}{% endblock %}
    </div>

    <footer>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark footer">
            <div class="container text-center">
                <a class="col-md-12 col-sm-12 col-xs-12 navbar-brand">-Desarrollo Web en entorno Servidor-</a>
            </div>
        </nav>
    </footer>
    

{% endblock %}
